- name: Update system
  apt:
    update_cache: yes
    upgrade: yes

- name: Install basic packages
  apt:
    name:
      - curl
      - git
      - ufw
      - unzip
      - tar
      - wget
      - ca-certificates
      - gnupg
    state: present
