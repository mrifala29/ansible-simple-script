- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install redis server
  apt:
    name: redis-server
    state: present

- name: Ensure redis is running
  service:
    name: redis-server
    state: started
    enabled: yes
